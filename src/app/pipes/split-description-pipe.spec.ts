import { SplitDescriptionPipe } from './split-description-pipe';

describe('SplitDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
